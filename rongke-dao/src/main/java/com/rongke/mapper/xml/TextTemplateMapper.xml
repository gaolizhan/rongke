<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rongke.mapper.TextTemplateMapper">

    <resultMap type="com.rongke.model.TextTemplate" id="mTextTemplate" autoMapping="true">
    </resultMap>

    <sql id="mColumnsSql">
        id AS id,
        text AS text,
        type AS type,
        title AS title,
        status AS status
    </sql>
</mapper>