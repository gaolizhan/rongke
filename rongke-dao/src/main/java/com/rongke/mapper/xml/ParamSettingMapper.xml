<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rongke.mapper.ParamSettingMapper">

    <resultMap type="com.rongke.model.ParamSetting" id="mParamSetting" autoMapping="true">
    </resultMap>

    <sql id="mColumnsSql">
        id AS id,
        limit_days AS limitDays,
        min_borrow_money AS minBorrowMoney,
        max_borrow_money AS maxBorrowMoney,
        interest_percent AS interestPercent,
        place_serve_percent AS placeServePercent,
        msg_auth_percent AS msgAuthPercent,
        risk_serve_percent AS riskServePercent,
        risk_plan_percent AS riskPlanPercent,
        allow_days AS allowDays,
        allow_percent AS allowPercent,
        overdue_percent AS overduePercent,
        gmt_datetime AS gmtDatetime,
        upt_datetime AS uptDatetime
    </sql>
</mapper>